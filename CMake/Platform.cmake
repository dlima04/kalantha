macro(kta_enumerate_build_platform)
  set(KTA_BUILD_PLATFORM ${CMAKE_HOST_SYSTEM_NAME})
  set(KTA_BUILD_IS_DARWIN OFF)
  set(KTA_BUILD_IS_LINUX OFF)
  set(KTA_BUILD_IS_POSIX OFF)
  set(KTA_BUILD_IS_WINDOWS OFF)
  set(KTA_BUILD_IS_CLANGCL_WINDOWS OFF)

  if(WIN32 OR KTA_BUILD_PLATFORM STREQUAL "Windows")
    set(KTA_BUILD_IS_WINDOWS ON)
  elseif(KTA_BUILD_PLATFORM STREQUAL "Linux")
    set(KTA_BUILD_IS_LINUX ON)
    set(KTA_BUILD_IS_POSIX ON)
  elseif(KTA_BUILD_PLATFORM STREQUAL "Darwin")
    set(KTA_BUILD_IS_DARWIN ON)
    set(KTA_BUILD_IS_POSIX ON)
  else()
    message(STATUS "Note -- unrecognized build platform.")
  endif()

  if(KTA_BUILD_IS_WINDOWS AND CMAKE_CXX_COMPILER_ID MATCHES "Clang")
    set(KTA_IS_CLANGCL_WINDOWS ON)
  endif()
endmacro()
